# ROLLBACK ONLY — 과거 Render·기타 레거시 백엔드로 전환 시 Cloudflare 대시보드 변수 예시입니다.
# Primary 운영은 AWS Amplify (docs/aws-cutover-runbook.md). 실제 URL은 콘솔에서만 설정하세요.

VITE_APP_ENV=production
VITE_API_BASE_URL=https://ROLLBACK-PUT-BACKEND-HTTPS-ORIGIN.example
VITE_AWS_API_URL=https://ROLLBACK-PUT-BACKEND-HTTPS-ORIGIN.example
