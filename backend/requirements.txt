# Python 3.10-3.12 recommended (3.13+ may lack wheels for some packages)
fastapi>=0.115.0,<1
uvicorn[standard]>=0.32.0,<1
python-multipart>=0.0.9
sqlalchemy>=2.0.0,<3
psycopg[binary]>=3.2.0,<4
passlib[bcrypt]>=1.7.4,<2
# passlib 1.7.x + bcrypt 4/5 는 버전 감지에서 "WARNING: (trapped) error
# reading bcrypt version" 경고가 뜨지만 해시·검증 기능은 정상 작동한다.
# chromadb 0.5.x 가 전이 의존성으로 bcrypt>=4.0.1 을 요구하므로 상한을
# 6 으로 열어 pip resolver 충돌을 해소한다 (이전 상한 <4 을 고수하면
# `ResolutionImpossible` 로 CI Install dependencies 가 실패).
bcrypt>=4.0.1,<6
python-jose[cryptography]>=3.3.0,<4
email-validator>=2.0.0,<3
python-dotenv>=1.0.0,<2
pandas>=2.0.0,<3
openpyxl>=3.1.0,<4
# 프로젝트 브리프 파일에서 텍스트 추출 (Word/Excel/PPT/PDF 등)
python-docx>=1.0.0,<2
python-pptx>=0.6.21,<2
pypdf>=4.0.0,<6
xlrd>=2.0.0,<3
odfpy>=1.4.0,<2
striprtf>=0.0.26,<1
numpy>=1.26.0,<3
scikit-learn>=1.4.0,<2
xgboost>=2.0.0,<3
matplotlib>=3.8.0,<4
joblib>=1.3.0,<2
psutil>=5.9.0,<6
torch
lightning
pytorch-forecasting
# Optional: AI chat (OpenAI / Gemini / Ollama OpenAI-compatible)
openai>=1.51.0,<2
google-generativeai>=0.8.0,<0.9
# RAG / vector database (used by backend/src/services/rag/*)
chromadb>=0.5.0,<0.6

# Colab 유사 노트북 (선택): NOTEBOOK_ENABLED=1 일 때 사용
jupyterlab>=4.0.0,<5

# Experiment V3 · 세션 보존 Python 커널(ipykernel) 의존성.
# jupyterlab 을 통해 전이 설치되어 있지만, V3 의 KernelManager 가
# 직접 의존하므로 버전 범위를 명시해 CI/배포 재현성을 확보한다.
ipykernel>=6.29,<7
jupyter_client>=8.6,<9
pyzmq>=25,<27
